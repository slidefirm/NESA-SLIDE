*.pptx filter=lfs diff=lfs merge=lfs -text

# Line endings
#
# 本專案多個 QA gate 以檔案位元組計算 sha256（renderer adapter manifest、
# layout preview QA ledger、editor source hash）。混合 CRLF/LF 會讓同一份內容
# 產生不同指紋，使那些 gate 只在特定機器的工作樹通過，在 CI 或新 clone 必定失敗。
#
# 專案的 Python 與 Node 產生器一律輸出 LF，因此倉庫與工作樹統一為 LF。
* text=auto eol=lf

# 以下類型不得做任何換行轉換
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.pdf   binary
*.zip   binary
*.mp4   binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.otf   binary

# Generated or third-party assets retain their original bytes.
demos/html/demo-deck.html -whitespace
prompt_system/renderers/html/assets/open-source-svg/illlustrations-co/*.svg -whitespace
references/design-research/20260726-breakthrough-morning-brief.md -whitespace
